services:
  mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: codex-mcp
    volumes:
      # Volume mount rules directory for immediate changes after git pull or edit
      - ./rules:/app/rules:ro
    ports:
      - '3627:3627'
    restart: unless-stopped

---
appliesTo:
  projects:
    - IgorKrupenja/rulekit-mcp
description: Rulekit MCP repo rules
---

## Markdown linting

- Always run `pnpm lint:markdown:check` after editing markdown in this repo.
- If it fails, fix issues (or run `pnpm lint:markdown`) before finishing.
